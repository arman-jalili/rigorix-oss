# Runbook: State Persistence Module

**Module:** `cli/src/state_persistence/` | **Version:** 0.1.0

Thin CLI wrapper over engine's state persistence. Handles `rigorix history` and `rigorix logs` state reads.

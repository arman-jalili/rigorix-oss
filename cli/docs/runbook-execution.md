# Runbook: Execution Engine Module

> **Module:** `cli/src/execution_engine/` | **Version:** 0.1.0

Thin CLI wrapper over the engine's parallel DAG executor. Handles execution dispatch, status polling, and abort.

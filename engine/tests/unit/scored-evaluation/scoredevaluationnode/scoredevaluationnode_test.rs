// Generated by Guardian TDD
// From: scoredevaluationnode
// Corrected (GAP-A-24): the tested type is now implemented — the
// is_defined / interacts_with_none assertions exercise the real type.

#[cfg(test)]
mod tests {
    use rigorix_engine::scored_evaluation::domain::Rubric;
    use rigorix_engine::scored_evaluation::domain::node::ScoredEvaluationNode;
    use std::collections::HashMap;
    use uuid::Uuid;

    #[test]
    fn test_scoredevaluationnode_is_defined() {
        let node = ScoredEvaluationNode::new(
            Uuid::new_v4(),
            serde_json::json!({}),
            Rubric::inline(serde_json::json!({})),
            "mock".to_string(),
            HashMap::new(),
            Default::default(),
        );
        assert_eq!(node.backend, "mock");
    }

    #[test]
    fn test_scoredevaluationnode_interacts_with_none() {
        let node = ScoredEvaluationNode::new(
            Uuid::new_v4(),
            serde_json::json!({}),
            Rubric::inline(serde_json::json!({})),
            "mock".to_string(),
            HashMap::new(),
            Default::default(),
        );
        let json = serde_json::to_string(&node).unwrap();
        assert!(serde_json::from_str::<ScoredEvaluationNode>(&json).is_ok());
    }
}

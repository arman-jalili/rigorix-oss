// Generated by Guardian TDD
// From: scoredevaluationerror
// Corrected (GAP-A-24): the tested type is now implemented — the
// is_defined / interacts_with_none assertions exercise the real type.

#[cfg(test)]
mod tests {
    use rigorix_engine::scored_evaluation::domain::ScoredEvaluationError;

    #[test]
    fn test_scoredevaluationerror_is_defined() {
        let err = ScoredEvaluationError::BackendNotFound("none".to_string());
        assert!(err.to_string().contains("none"));
    }

    #[test]
    fn test_scoredevaluationerror_interacts_with_none() {
        assert!(matches!(
            ScoredEvaluationError::BackendError("x".to_string()),
            ScoredEvaluationError::BackendError(_)
        ));
    }
}

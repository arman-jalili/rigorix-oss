// Generated by Guardian TDD
// From: scoreabove-policy-condition
// Corrected (GAP-A-24): the tested type is now implemented — the
// is_defined / interacts_with_none assertions exercise the real type.

#[cfg(test)]
mod tests {
    use rigorix_engine::scored_evaluation::domain::Rubric;
    use rigorix_engine::scored_evaluation::domain::node::ScoredEvaluationNode;
    use std::collections::HashMap;
    use uuid::Uuid;

    #[test]
    fn test_scoreabove_policy_condition_is_defined() {
        let mut thresholds = HashMap::new();
        thresholds.insert("correctness".to_string(), 0.8);
        let node = ScoredEvaluationNode::new(
            Uuid::new_v4(),
            serde_json::json!({}),
            Rubric::inline(serde_json::json!({})),
            "mock".to_string(),
            thresholds,
            Default::default(),
        );
        assert_eq!(node.thresholds.get("correctness"), Some(&0.8));
    }

    #[test]
    fn test_scoreabove_policy_condition_interacts_with_none() {
        let mut thresholds = HashMap::new();
        thresholds.insert("quality".to_string(), 0.9);
        let node = ScoredEvaluationNode::new(
            Uuid::new_v4(),
            serde_json::json!({}),
            Rubric::inline(serde_json::json!({})),
            "mock".to_string(),
            thresholds,
            Default::default(),
        );
        let json = serde_json::to_string(&node).unwrap();
        assert!(json.contains("thresholds"));
    }
}

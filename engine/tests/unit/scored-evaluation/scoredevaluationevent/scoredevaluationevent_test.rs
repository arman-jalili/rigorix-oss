// Generated by Guardian TDD
// From: scoredevaluationevent
// Corrected (GAP-A-24): the tested type is now implemented — the
// is_defined / interacts_with_none assertions exercise the real type.

#[cfg(test)]
mod tests {
    use chrono::Utc;
    use rigorix_engine::scored_evaluation::domain::event::ScoredEvaluationEvent;
    use uuid::Uuid;

    #[test]
    fn test_scoredevaluationevent_is_defined() {
        let event = ScoredEvaluationEvent::ScoredEvaluationStarted {
            node_id: Uuid::new_v4().to_string(),
            execution_id: Uuid::new_v4(),
            backend: "mock".to_string(),
            timestamp: Utc::now(),
        };
        assert!(serde_json::to_string(&event).is_ok());
    }

    #[test]
    fn test_scoredevaluationevent_interacts_with_none() {
        let event = ScoredEvaluationEvent::ScoredEvaluationStarted {
            node_id: Uuid::new_v4().to_string(),
            execution_id: Uuid::new_v4(),
            backend: "mock".to_string(),
            timestamp: Utc::now(),
        };
        let json = serde_json::to_string(&event).unwrap();
        assert!(serde_json::from_str::<ScoredEvaluationEvent>(&json).is_ok());
    }
}

// Generated by Guardian TDD
// From: domain-layer-domain
// Corrected (GAP-A-24): the tested type is now implemented — the
// is_defined / interacts_with_none assertions exercise the real type.

#[cfg(test)]
mod tests {
    use rigorix_engine::scored_evaluation::domain::Rubric;

    #[test]
    fn test_domain_layer_domain_is_defined() {
        let rubric = Rubric::inline(serde_json::json!({"quality": 0.9}));
        assert!(serde_json::to_string(&rubric).is_ok());
    }

    #[test]
    fn test_domain_layer_domain_interacts_with_none() {
        let rubric = Rubric::inline(serde_json::json!({"quality": 0.9}));
        let json = serde_json::to_string(&rubric).unwrap();
        assert!(serde_json::from_str::<Rubric>(&json).is_ok());
    }
}

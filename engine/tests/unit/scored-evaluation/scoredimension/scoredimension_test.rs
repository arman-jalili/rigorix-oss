// Generated by Guardian TDD
// From: scoredimension
// Corrected (GAP-A-24): the tested type is now implemented — the
// is_defined / interacts_with_none assertions exercise the real type.

#[cfg(test)]
mod tests {
    use rigorix_engine::scored_evaluation::domain::ScoreDimension;

    #[test]
    fn test_scoredimension_is_defined() {
        let dim = ScoreDimension::new(0.8, 1.0, "Quality", true);
        assert_eq!(dim.score, 0.8);
        assert!(dim.passed);
    }

    #[test]
    fn test_scoredimension_interacts_with_none() {
        let dim = ScoreDimension::new(0.8, 1.0, "Quality", true);
        let json = serde_json::to_string(&dim).unwrap();
        assert!(serde_json::from_str::<ScoreDimension>(&json).is_ok());
    }
}

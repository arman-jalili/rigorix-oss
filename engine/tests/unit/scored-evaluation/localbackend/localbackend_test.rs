// Generated by Guardian TDD
// From: localbackend
// Corrected (GAP-A-24): the tested type is now implemented — the
// is_defined / interacts_with_none assertions exercise the real type.

#[cfg(test)]
mod tests {
    use rigorix_engine::scored_evaluation::domain::backend::ScoringBackend;
    use rigorix_engine::scored_evaluation::infrastructure::backends::LocalBackend;

    #[test]
    fn test_localbackend_is_defined() {
        let backend = LocalBackend::new("/tmp/score.sh", 5000);
        assert_eq!(backend.backend_name(), "local");
    }

    #[test]
    fn test_localbackend_interacts_with_none() {
        let backend = LocalBackend::new("/tmp/score.sh", 5000);
        let _ = &backend as &dyn ScoringBackend;
    }
}

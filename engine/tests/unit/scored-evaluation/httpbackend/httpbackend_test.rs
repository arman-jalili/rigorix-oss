// Generated by Guardian TDD
// From: httpbackend
// Corrected (GAP-A-24): the tested type is now implemented — the
// is_defined / interacts_with_none assertions exercise the real type.

#[cfg(test)]
mod tests {
    use rigorix_engine::scored_evaluation::domain::backend::ScoringBackend;
    use rigorix_engine::scored_evaluation::infrastructure::backends::HttpBackend;
    use std::collections::HashMap;

    #[test]
    fn test_httpbackend_is_defined() {
        let backend = HttpBackend::new("http://localhost:9999/score", HashMap::new(), 5000);
        assert_eq!(backend.backend_name(), "http");
    }

    #[test]
    fn test_httpbackend_interacts_with_none() {
        let backend = HttpBackend::new("http://localhost:9999/score", HashMap::new(), 5000);
        let _ = &backend as &dyn ScoringBackend;
    }
}

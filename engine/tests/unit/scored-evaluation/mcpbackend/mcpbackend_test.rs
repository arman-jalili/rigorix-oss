// Generated by Guardian TDD
// From: mcpbackend
// Corrected (GAP-A-24): the tested type is now implemented — the
// is_defined / interacts_with_none assertions exercise the real type.

#[cfg(test)]
mod tests {
    use rigorix_engine::scored_evaluation::domain::backend::ScoringBackend;
    use rigorix_engine::scored_evaluation::infrastructure::backends::McpBackend;

    #[test]
    fn test_mcpbackend_is_defined() {
        let backend = McpBackend::new("http://localhost:9999/mcp", 5000);
        assert_eq!(backend.backend_name(), "mcp");
    }

    #[test]
    fn test_mcpbackend_interacts_with_none() {
        let backend = McpBackend::new("http://localhost:9999/mcp", 5000);
        let _ = &backend as &dyn ScoringBackend;
    }
}
